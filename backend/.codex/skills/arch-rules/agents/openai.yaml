interface:
  display_name: "아키텍처 규칙 고정"
  short_description: "절대 규칙과 자원 점유 금지 패턴을 ArchUnit 테스트로 고정합니다"
  default_prompt: "Use $arch-rules to encode the project's architecture and resource rules as ArchUnit tests."
