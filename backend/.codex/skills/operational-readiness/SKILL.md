---
name: operational-readiness
description: "결제·외부 연동·배포·데이터·대규모 변경의 장애 대응 준비도를 근거 기반으로 점검하고, 운영 위험 레지스터·복구·관측성·Runbook 공백을 보고할 때 사용한다. 일반 코드 스타일 리뷰에는 사용하지 않는다."
---

이 skill의 지시는 `.claude/commands/operational-readiness.md`에 있다. **그 파일을 처음부터 끝까지 읽고 그대로 따른다.**

- 지시가 "커맨드"라고 표현한 것은 이 skill을 가리킨다.
- `$ARGUMENTS`는 사용자가 이 skill에 넘긴 인자로 읽는다.
- 시작 시 `.codex/AGENTS.md`를 읽고, 그 파일이 단일 원본으로 지정한 `.claude/CLAUDE.md`도 반드시 읽는다.
- 공통 참조 문서는 `.claude/skills/references/` 아래에 있다.

원본을 이 파일로 복사해 오지 않는다. 사본을 만들면 두 곳이 어긋난다.
