# Codex instruction bridge

Before doing any work in this repository, read and follow `.codex/AGENTS.md` completely.

Reusable workflows are stored under `.codex/skills/` and exposed through `.agents/skills`.
When a request names a skill or matches its description, read its `SKILL.md` completely before acting.
